// const { writeFile } = require("fs");

const fs = require('fs');

function writeRender(employee) {
    console.log(employee);
    var i;
for (i = 0; i < employee.length; i++) {
    console.log(employee[i]);
    if (employee[i] === 'Engineer')
    }
}
module.exports = writeRender